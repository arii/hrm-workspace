# The URL of the repository to attach the runner to
REPO_URL=https://github.com/arii/hrm

# Option 1: Use a static Runner Token
# Get this from Repo Settings -> Actions -> Runners -> New Runner
# Note: These tokens expire after a short period.
RUNNER_TOKEN=

# Option 2: Use a Personal Access Token (PAT) to fetch the runner token dynamically
# This is more robust as it allows the runner to re-register automatically.
# The PAT requires the 'repo' scope (for private repos) or 'public_repo' scope.
GH_TOKEN=
